# FDM-201 worker-context package: GOAL-EXE-10 (PyBC ligand /
# precursor reproduction, WP-10).
#
# Context a worker needs to execute GOAL-EXE-10 without unrelated
# project context, per schemas/worker-context.schema.yaml. The
# referenced goal/assumption/parameter/protocol ids resolve to the
# frozen FDM-201 benchmark data.

record_kind: benchmark_worker_context_package
$schema: "https://json-schema.org/draft/2020-12/schema"
context_id: "sr_wctx_fdm201_goal_exe_10"
worker_role: experiment_worker
goal_id: "GOAL-EXE-10"
goal_version: "v1"
run_id: null
context_hash: null
execution_package_refs: ["sr_package_fdm201_exp01_pybc_ligand"]
source_refs: ["sr_source_86450d48aa337786cad25c306944daae"]
evidence_refs: ["EV-INV-0701", "EV-INV-0702", "EV-INV-0703", "EV-INV-0704", "EV-INV-0705", "EV-INV-0706"]
upstream_result_refs: []
protocol_refs: ["ANL-010"]
resource_refs: ["RES-013", "RES-017"]
assumption_refs: ["ASM-A1-N-01"]
ambiguity_refs: []
environment:
  runtime: "lab filesystem protocol runner"
  workspace_root: "fdm201-lab/exp01-pybc-ligand/"
  records: "run-records/ raw-records/ batch-records/ identity-evidence/ handoff/"
required_outputs:
  - "handoff/lot-records.yaml"
  - "handoff/exp01-handoff-manifest.yaml"
  - "batch-records/batch-statistics.yaml"
  - "deviation-register.yaml"
allowed_actions:
  - "execute the steps of the linked execution package in order"
  - "read the inputs listed in the linked execution package"
  - "write records only under the package workspace"
  - "register every deviation in the deviation register"
forbidden_actions:
  - "modify frozen benchmark data outside the package workspace"
  - "change procedure parameters without a registered deviation"
  - "access resources outside the linked execution package"
  - "delete or cherry-pick run records"
