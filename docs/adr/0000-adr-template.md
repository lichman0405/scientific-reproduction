# ADR-0000 — <Short decision title>

> This file is the ADR template. Copy it to `<NNNN>-<short-slug>.md`
> (e.g. `0001-lab-adapter-filesystem-handoff.md`), fill in the fields, and
> replace the placeholder tokens (`ADR-0000`, `<...>`, `YYYY-MM-DD`).
> See `docs/adr/README.md` for when an ADR is required, the lifecycle, and
> numbering. The template itself is never a decision.

- **Status:** proposed <!-- proposed | accepted | superseded -->
- **Date:** YYYY-MM-DD
- **Reviewer:** <PR reviewer> <!-- always an independent reviewer; authors never self-approve -->
- **Related PR:** #<NNN> <!-- the pull request that ships this ADR, or "none" -->
- **Supersedes:** <!-- ADR-NNNN when this ADR replaces an earlier one, else "none" -->
- **Superseded by:** <!-- ADR-NNNN when a later ADR replaces this one, else "none" -->

## Context

<!-- The forces at play and the situation that makes a decision necessary.
     Reference the locked decision(s) affected (e.g. decision N or ADR-DEV-0NN
     in 20-ARCHITECTURE-DECISIONS.md), and any relevant specification section.
     If this ADR records an implementation deviation from a locked decision,
     state that explicitly and link the change request. -->

## Decision

<!-- What is being decided, precisely and unambiguously. If this ADR is a
     deviation, state how the implementation will differ and what the locked
     decision continues to require. -->

## Consequences

<!-- Positive consequences of accepting this decision. -->

<!-- Negative / trade-off consequences, including anything the implementation
     must guard against. -->

## Verification notes

<!-- How a reviewer can verify that the implementation matches this decision.
     "n/a" if not applicable. -->
