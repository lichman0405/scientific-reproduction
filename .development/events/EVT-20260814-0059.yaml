event_id: EVT-20260814-0059
timestamp: '2026-08-14'
type: GOAL_MERGED
goal_id: DEV-M7-G03
attempt_id: DEV-M7-G03-a1
pr_number: 53
head_sha: c29b5e6a3cd839ee4e074d0d15e3a22955c88340
merge_sha: 60f8c1e6f04ac9f04827e02a73169d1d987ab036
summary: >-
  DEV-M7-G03 merged (SSH compute transport adapter): 2 new files +3419/-0
  (src/adapters/compute/ssh.py 1912; tests/adapters/compute/
  test_ssh_adapter.py 1508). Review DEV-M7-G03-a1 PASS (no defects).
  AC-01 structural failure classification: SSHTransportError hierarchy
  (connection/transfer/timeout) is the retryable TRANSPORT class
  (failure_class="transport" persisted via classify_transport_failure);
  SSHRemoteError is a clean remote answer never retried (missing remote
  output -> ComputeCollectError, launch refusal -> SSHJobLaunchError with
  failure_class="job"); _execute retries exactly max_attempts under the
  injected backoff; _transition is pure (exit 0 -> completed clears stale
  classification; non-zero -> failed with "remote command exited with
  status <N>"; None -> completed + SSH_EXIT_CODE_UNAVAILABLE_NOTE);
  healthy probes clear stale transport markers; classifications survive
  re-hydration. AC-02 credentials never persisted: SSHCredentials is
  frozen with NO serialization methods (asserted), the durable record
  schema is exactly the 15 documented non-credential keys (asserted),
  a full-cycle walk of every state file + transport log finds no
  credential value, launch commands embed none. AC-03 mock-SSH reconnect:
  scripted FakeTransport (no network/sleeps/randomness), reconnect after
  connect failures, mid-operation drops retried with exact
  connect/run/disconnect sequences, retry count exactly max_attempts (2
  and 5), RecordingBackoff 1-based attempts with zero delays, dropped
  probes/pulls recover, failing disconnect never masks the operation,
  default_backoff exponential capped at 30s. Six-operation surface
  mirrors DEV-M7-G02: durable records at <state_dir>/jobs/<job_id>.json,
  terminal states record-driven, collect registers artifacts through the
  REAL ArtifactRegistry (deterministic ids, real SHA-256/size, producer
  stamp adapter:compute/ssh@v1.0, idempotent re-collection), cancel local
  for prepared / remote kill for running (SIGTERM 143), resume from the
  record alone; launch wrapper (mkdir/cd/nohup/sh -c with `; echo $? >
  status` capture -- worker self-caught the missing capture pre-report,
  verified on head), shlex.quote everywhere, RemotePath safe segments
  (FND-M9-G02-01). Verification: -k ssh 49 passed; tests/adapters 184;
  ruff clean; mypy 80 head/80 scratch; contracts PASS; scratch full
  suite 2441 passed, 6 skipped, 1 deselected (= 2392 + 49, exact
  prediction; merged state == head since branch parent a809187 ==
  origin/main); CI both jobs PASS. Branch parent a809187 (dispatch-time
  origin/main; direct ancestor; no rebase); M11-G03 merged mid-attempt
  with fully disjoint files. Zero type: ignore / noqa in both new files.
  Merged count 52/80.
