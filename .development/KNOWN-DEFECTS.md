# Known Spec-Package Defects & Dispositions

| ID | File | Defect | Disposition |
|---|---|---|---|
| SPEC-DEFECT-001 | `development/validate_development_spec.py` (lines ~40–41) | Stale assertion: "Expected 79 development goals, found 80". The spec package was upgraded 79→80 goals in v0.1.2 (see `SPEC-CHANGELOG.md`); `validate_spec_package.py`, `PACKAGE-MANIFEST.json` and the goal catalog all agree on 80. The standalone validator was not updated. | **FIX IN DEV-M0-G00 (IN_CURRENT_GOAL)** — DEV-M0-G00 AC-05 requires `scripts/validate_development_contracts.py` to pass on the frozen 80-goal contract set; a validator that asserts the wrong count cannot pass. The fix (79→80) is strictly a defect repair, does not broaden M0-G00 scope, and does not modify any frozen acceptance criterion. Recorded here as the mandated decision trail. |

| DEV-DEFECT-001 | `src/scientific_reproduction/core/leases.py` (`_compare_and_unlink`), `core/locks.py` (`FileLock.release`, `_break_stale`) | **TOCTOU race at the expiry boundary (security review, HIGH/MEDIUM)**. The read-verify-then-unlink sequences are not atomic: a holder renewing/releasing at the exact expiry instant can have its fresh record removed by a claimer's takeover unlink that landed after verification (and vice versa). Worst case: transient dual ownership — two principals briefly believe they hold the lease/lock. Impact: never corrupts persisted state (the create step is always atomic create-if-absent, so at most one record exists on disk at any instant); the loser detects the loss on its very next lease/lock operation (record ≠ its grant → LeaseHeldError); requires a nanosecond interleaving between concurrent processes at the exact boundary. The deterministic tests (injected clock) cannot exercise the true wall-clock race. Proper fix: OS advisory locks (fcntl.flock / msvcrt.locking) on a never-unlinked per-object control file, or inode-bound removal (O_NOFOLLOW + fstat) — a cross-platform design change to a merged, reviewed component, outside every remaining v0.1 goal's frozen scope. | **ACCEPTED-RISK FOR v0.1, DOCUMENTED** — recorded in DEV-M1-G03 review as a known limitation; hazard window, invariant ("at most one record on disk; loser detects on next operation"), and detection semantics documented in module docstrings. Fix is a backlog candidate (v0.1.1; DEVELOPMENT_CHANGE_REQUEST-B if prioritized). No frozen acceptance criterion is modified. |

| DEV-DEFECT-002 | `tests/state/test_leases.py` (`test_concurrent_takeover_of_expired_lease_single_winner`) | **Timing-flaky test harness over-assumption (observed ~1/10 runs on Windows-local at DEV-M1-G06 review; CI green on ubuntu; full-suite rerun clean)**. The test forces both claimers into the expiry-boundary takeover race via an interleaved `os.link` barrier, then asserts **exactly one** winner. Under adversarial interleaving the bounded 3-pass re-decision loop in `LeaseStore.acquire` (leases.py:413) can exhaust for BOTH claimants (the DEV-DEFECT-001 compare-and-unlink TOCTOU family), so both receive the documented transient `LeaseHeldError("contested by concurrent claimers; retry the acquisition")` and neither writes — zero winners, nothing persisted, no dual ownership, no corruption (the safe direction; the product contract guarantees **at most** one winner, never that the loop resolves within 3 passes). Pre-existing: `tests/state/` is byte-identical between the M1-G05 base and the M1-G06 head, and M1-G06 ships zero production changes. | **ACCEPTED, DOCUMENTED, FIX OUT OF FROZEN SCOPE** — `tests/state/` is not in DEV-M1-G06's `allowed_scope`, so no M1-G06 workstream may modify this test; the failing direction is safe and retry-able. Remediation bundled with DEV-DEFECT-001 (advisory-lock redesign removes the TOCTOU the flake exercises) or a dedicated flake-hardening follow-up (v0.1.1 backlog; DEVELOPMENT_CHANGE_REQUEST-B if prioritized). No frozen acceptance criterion is modified. |

Neither defect alters any frozen acceptance criterion or DEV-GOAL contract.
