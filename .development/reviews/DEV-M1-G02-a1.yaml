goal_id: DEV-M1-G02
attempt_id: DEV-M1-G02-a1
reviewer: development-supervisor
decision: PASS
criteria:
  - criterion_id: AC-01
    result: PASS
    evidence: >-
      FilesystemStateBackend persists exactly one JSON file per object at
      base_dir/<obj_type>/<object_id>.json (state_backend.py _object_path);
      no monolithic mutable state file exists — tests assert the base dir
      root holds only per-type directories and every file is a per-object
      <id>.json. Independent rerun at head 1259702: 61 passed, 6
      platform-conditioned skips, exit 0.
  - criterion_id: AC-02
    result: PASS
    evidence: >-
      Every write goes through core/atomic.py atomic_write: unique
      same-directory mkstemp file, write+flush+os.fsync, then os.replace
      (atomic on Windows and POSIX); any failure unlinks the temp and leaves
      the previous target untouched. Tests simulate crash-before-replace
      (monkeypatched os.replace raising after temp fully written) and prove
      the last valid object survives, including first-write and stale-temp
      cases; backend-level interrupted-write test asserts previous object
      bytes intact.
  - criterion_id: AC-03
    result: PASS
    evidence: >-
      write() rejects non-dict content (TypeError) and validates against
      schemas/<obj_type>.schema.yaml via validate_and_reject, raising
      SchemaValidationError BEFORE anything is persisted (invalid enum /
      missing required / range violations tested; no directories created).
      Unknown obj_type raises UnknownObjectTypeError. Canonical
      serialization (sort_keys, indent=2, UTF-8) makes rewrites byte-
      identical; round-trip parametrized over all 21 normative object types
      (read() == written, validate_object(read()) == []).
verification_rerun:
  - command: python -m pytest -q tests/state
    exit_code: 0
  - command: python -m ruff check .
    exit_code: 0
  - command: python -m mypy src/
    exit_code: 0
comments:
  - >-
    Scope check: 4 files changed (core/atomic.py, core/state_backend.py,
    tests/state/test_atomic.py, tests/state/test_state_backend.py) — all
    inside allowed_scope; frozen paths untouched; two conventional commits
    with [DEV-M1-G02], no force-push, no amend.
  - >-
    Security review findings (3 MEDIUM, automated) addressed on-branch via
    follow-up commit 1259702, all within allowed scope: (1) unconditional
    0o644 chmod removed — atomic_write gains explicit file_mode param,
    None = no chmod (mkstemp 0o600, umask respected), explicit mode chmod
    raises on OSError instead of swallowing; (2) read() refuses symlinked
    object files and any resolved path escaping base_dir.resolve() before
    parsing; write() verifies resolved-parent containment before staging —
    symlink tests run on Linux CI (skip on Windows where symlink creation
    lacks privilege; worker additionally verified via directory junction
    on this host); (3) case-insensitive ID collisions rejected as policy on
    every platform (deterministic cross-OS behavior), exact-case rewrites
    allowed. Tests cover each fix. Residual accepted risk: list_ids/delete
    do not apply the containment guard (read is the guarded access path;
    unlink on a symlink removes the link, not the target) — documented in
    module docstring.
  - >-
    Forbidden-dependency check: no SQLite/PostgreSQL introduced; v0.1
    ships exactly the filesystem backend; interface documented for future
    backends per 14-STATE-GIT-ARTIFACTS.md.
  - >-
    Pyright "unreachable code" note at state_backend.py:240 is a
    deliberate runtime contract check (non-dict rejection for dynamically
    typed callers) — comment added; mypy (the configured gate) green on 21
    source files.
  - >-
    Worker followed contract: conventional commits, draft PR #7 with
    template fields, security-hardening section added to PR description,
    CI both jobs PASS at reviewed head, no self-approval, no merge.
git_review:
  pr_number: 7
  reviewed_head_sha: 125970266a2ae150578e4b52afed1da5ab6ad066
  ci_status: PASS
  scope_check: PASS
  merge_authorized: true
  merge_method: squash
