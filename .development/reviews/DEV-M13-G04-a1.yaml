review_id: DEV-M13-G04-a1
goal_id: DEV-M13-G04
attempt_id: DEV-M13-G04-a1
reviewer: Development Supervisor (independent review on PR head)
reviewed_at: '2026-08-14'
decision: PASS

pr:
  number: 80
  base: main
  base_oid: 784e13a5729dcb19cfc8761ad4aff23e707e596e
  head_ref: feat/dev-m13-g04-docs
  head_oid: 45e08116538b6bffe4b6057b4d52803c63d3a92e
  commits: 1
  files: 5 (+819/-0): README.md (quickstart + doc links, +13),
    docs/user/installation.md (125), docs/user/reproduce-and-goals.md (256),
    docs/user/monitor-and-handoff.md (239), docs/operations/adapters-slurm.md
    (186) -- all docs under README/docs/user/docs/operations; src/ and tests/
    byte-identical to origin/main (git diff HEAD origin/main -- src tests is
    empty); no .development changes, no AC changes, no --no-verify.

acceptance_criteria:
  AC-01:
    status: PASS
    evidence:
    - >-
      supervisor read of the full guide set plus live re-execution of the
      documented commands: installation.md and the README quickstart ground
      every command in real repo state -- pyproject.toml requires-python
      >=3.11, stdlib-only runtime (dependencies = []), the dev-extra toolchain
      (pytest/pytest-cov/ruff/mypy/jsonschema/PyYAML), scripts/verify.py as the
      canonical one-shot entry (pytest + ruff + mypy, [verify] PASS/FAIL), the
      manual gate commands, the FDM-201 validator path, and the 3181-test
      arithmetic (3063 baseline + 50 benchmark-acceptance + 33 audit/trace +
      18 report + 17 summary) which matches the merged baseline exactly.
    - >-
      independent battery on the head reproduced the documented numbers: full
      suite 3181 passed in 329.61s, ruff clean, mypy clean (115 files), the
      CLI sanity command `python -m scientific_reproduction.cli.reproduce init
      10.1039/D5TA00771B --root <tmp>` exits 0 with the documented output
      shape. The troubleshooting entries (absolute interpreter path on
      PowerShell, double-pytest harness quirk, DEV-DEFECT-002 lease flake)
      match the accepted observations recorded in the M13 reviews.
    notes: >-
      AC-01 "a new implementer can install, run and test from the docs" is
      evidenced by full-command execution fidelity: every documented command
      was executed during the review with the documented result, including a
      fresh throwaway project initialization.
  AC-02:
    status: PASS
    evidence:
    - >-
      supervisor read of monitor-and-handoff.md with ~20 API claims
      grep-verified against src: the durable-state table (watched/<run>.json,
      checkpoint.json, heartbeat.json, trigger/<run>.json, run/<run>.json via
      FilesystemStateBackend, event/ via ProjectEventLog) matches
      monitoring/registry.py, checkpoint.py, triggers.py and core/
      state_backend.py; derive_monitor_id (registry.py:138) is confirmed a
      pure function of the state directory; MonitorRecovery.reconstruct
      (recovery.py:481) / resume_engine (recovery.py:597) and the
      CorruptRecoveryStateError / RecoveryContractError refusal behavior match
      the code; COMPLETION_SIGNALS is exactly
      frozenset({"RESULT_AVAILABLE"}) (reconcile.py:173) so the default probe
      can never fabricate completion; ENGINEERING_RETRY_WHITELIST =
      {"transport"} (retry.py:171) with the stable decision reasons
      engineering_failure_retry_authorized / scientific_failure_retry_refused
      (retry.py:196/199) and the engineering_retry_decision event
      (retry.py:187); the filesystem LabAdapter flow (dispatch behind
      validate_and_reject against schemas/lab-execution-package.schema.yaml,
      exactly-once dispatch_id as a deterministic pure function, status
      RUNNING_EXTERNAL -> RESULT_AVAILABLE, collect refusal via
      evaluate_result_manifest / CollectionRejectedError, missing-result-
      request.json in the outgoing directory) matches adapters/lab/
      filesystem.py verbatim; the analysis-side claims (freeze_primary_protocol
      / revise_protocol / protocol_lineage, register_result / read_result /
      list_results, UnresolvedResultReferenceError) verified in
      analysis/protocols.py:1346 and analysis/results.py.
    - >-
      spec citations spot-checked: 15-ADAPTER-SPEC.md SS5 lists
      resume_session(session_ref) and expose_command(command_spec) at
      L52/L55; 01-PRODUCT-REQUIREMENTS.md L95 carries the quoted sentence
      "Platform adapters may expose these as slash commands or equivalent
      commands:"; the recovery levels L1-L4 and the TERMINATION_GATE (SS6)
      match core/models.py:311 and the 08-STRICT-RECOVERY-CLOSURE references
      established in the M13-G02/G03 reviews.
    notes: >-
      AC-02 "Monitor recovery and filesystem Lab handoff documented" is
      evidenced by the file-by-file grounding of the durable-state table,
      the recovery model and the handoff layout -- every API name and event
      type grep-verified against the real modules.
  AC-03:
    status: PASS
    evidence:
    - >-
      supervisor read of adapters-slurm.md with the constructor signature
      verified verbatim against slurm_ssh.py:1369-1379 (credentials: SSHCredentials,
      state_dir: str | Path, keyword-only transport: SSHTransport required
      with no default, modules: tuple[str, ...] = (), environment:
      Mapping[str, str] | None = None, retry_policy: SSHRetryPolicy | None =
      None defaulting to SSHRetryPolicy(), now: Clock | None = None defaulting
      to utc_now); default_backoff confirmed exactly
      min(30.0, 0.5 * (2 ** (attempt - 1))) (ssh.py:587) with max_attempts = 3
      (ssh.py:606); SLURM_STATE_RULES confirmed as R-SLURM-S1..S27 with
      R-SLURM-S27 the trailing total default (slurm_ssh.py:391-527) and the
      squeue %T vocabulary (PD/CF/S/R/CG/RQ) and sacct State vocabulary
      (CD/CA/F/TO/NF/OOM/PR) matching the rule table; SLURM_STATE_UNAVAILABLE_NOTE
      (slurm_ssh.py:209) verified as the neither-squeue-nor-sacct fallback;
      ADAPTER_ID "compute/slurm_ssh" and the scripts/<job_id>.slurm.sh staging
      path verified; the module contains no paramiko import.
    - >-
      every documented v0.1 limitation traced to real code: constructor-bound
      configuration (no config-file loader, no env-var reads -- the __init__
      takes every knob as an argument), no shipped SSH transport (SSHTransport
      is a pure ABC, ssh.py:507), queue-blind probing (status derives its
      --jobs argument from the recorded external id only; squeue/sacct/scancel
      verified), sbatch invoked with --chdir/--output only (no
      partition/account/GPU/reservation flags), no exactly-once launch across
      a mid-operation drop (reconnect re-runs the pending step), credentials
      in memory only with no credential fields in durable records (AC-02 of
      DEV-M7-G03), per-operation connect/disconnect sessions.
    notes: >-
      AC-03 "Slurm-over-SSH configuration and limitations documented" is
      evidenced by the verbatim-verified constructor signature, the rule-table
      vocabulary check and the code-traced limitation list -- the guide states
      precisely what the adapter cannot do, all confirmed by the source.
      Credentials-never-persist (M7-G03 AC-02) is restated correctly.

findings: []

observations:
  - id: OBS-M13-G04-01
    detail: >-
      mypy reports 115 source files in this review run (.post-merge-venv)
      versus 114 in the M13-G03 review venv -- with no src change in this
      goal (docs-only, src byte-identical to main). The count is
      mypy/venv-environment dependent; the worker's 115 report is
      independently confirmed and clean either way. Recorded so the Release
      Gate does not treat a count delta as a regression.
  - id: OBS-M13-G04-02
    detail: >-
      CLI determinism live-verified during review: two independent
      `python -m scientific_reproduction.cli.reproduce init 10.1039/D5TA00771B
      --root <tmp-a|tmp-b> --title FDM-201 --timestamp 2026-08-14T00:00:00Z`
      runs produced byte-identical output including the exact project id
      sr_project_83f57b0b35f5235095c17318d1a9601b and git checkpoint
      aca01e900f52 -- matching the verified example in reproduce-and-goals.md
      exactly. The determinism claims (core.ids.generate_id over target form
      + identifier, pinned author identity, pinned timestamps) hold in
      practice.
  - id: OBS-M13-G04-03
    detail: >-
      The /goals-no-CLI honesty finding is verified accurate: no /goals CLI
      module exists in the v0.1 codebase. The guide's positioning -- the
      /goals views as pure functions of the planning primitives plus the
      RoleContract descriptor layer of adapters/platform/contracts/base.py
      (CoreStateApi/DecisionAuthority/VerdictAuthority/RetryAuthority, Action
      sets, forbidden_practices -- grep-verified), with the SS6 quote
      (01-PRODUCT-REQUIREMENTS.md L95) and the expose_command contract of
      15-ADAPTER-SPEC.md SS5 (L55) -- matches CLAUDE-CODE-HANDOFF.md section 5
      (native /goal on the Supervisor, frozen contracts to teammates).
      Honest documentation of the v0.1 surface; no gap vs the contract.
  - id: OBS-M13-G04-04
    detail: >-
      The double-pytest-process phenomenon did NOT recur in this review run
      (single summary line in the log; contrast OBS-M13-G02-03 /
      OBS-M13-G03-03). The phenomenon is harness-dependent and not
      deterministic; release-gate interpretation should not rely on its
      occurrence either way.

verification:
  head: 45e08116538b6bffe4b6057b4d52803c63d3a92e (PR #80)
  commands:
  - "pytest -q tests (supervisor independent background run on head, .post-merge-venv; src/tests byte-identical to main -- `git diff HEAD origin/main -- src tests` empty) -> 3181 passed, 7 skipped, 1 deselected in 329.61s, exit 0 (3063 baseline + 50 benchmark-acceptance + 33 audit/trace + 18 report + 17 summary). No DEV-DEFECT-002 flake occurrence."
  - "ruff check . (the CI invocation) -> clean, exit 0"
  - "mypy src -> 115 files, no issues, exit 0"
  - "python benchmarks/fdm201/execution_packages/validate_packages.py -> 20/20 PASS, exit 0"
  - "CLI live determinism check: reproduce init 10.1039/D5TA00771B with pinned --timestamp, two independent roots -> both exit 0, byte-identical output (sr_project_83f57b0b35f5235095c17318d1a9601b / git checkpoint aca01e900f52) matching the documented verified example"
  - "CI on head 45e0811: Test, lint, and type check PASS (3m51s) + Validate development contracts PASS (11s); CodeRabbit skipped on draft"
  - "~40 API/citation claims in the four guides spot-checked against src via grep (planning init/plan/audit/freeze/dag; workers context; monitoring registry/recovery/reconcile/retry; adapters lab filesystem/manifest + compute ssh/slurm_ssh + platform contracts; analysis protocols/results; core models/events/audit git; cli reproduce flags) -- all hit; two spec quotes verified in 15-ADAPTER-SPEC.md and 01-PRODUCT-REQUIREMENTS.md"
  post_merge: >-
    (filled after squash merge)

design_review:
  decomposition: >-
    One documentation deliverable decomposed into five files: README
    quickstart links, installation.md (requirements, editable install, the
    verify.py canonical entry, manual gates, FDM-201 validator,
    troubleshooting), reproduce-and-goals.md (/reproduce CLI surface + the
    honest /goals-as-planning-primitives surface), monitor-and-handoff.md
    (Monitor durable state, recovery model, reconciliation/retry/triggers,
    scientific recovery levels, filesystem Lab handoff), adapters-slurm.md
    (identity, constructor-bound configuration, operations, state
    normalization, durable layout, failure classification, limitations,
    grounding). The docs mirror the established house style (every claim
    grounded in a code path or spec section).
  real_machinery: >-
    Every command and API name was executed or grep-verified against the real
    code during the review; the CLI example was re-executed and is
    byte-identical; the limitations list in adapters-slurm.md traces each item
    to constructor-bound / state-file reality (no invented capabilities, no
    invented config surfaces); the /goals section documents exactly what v0.1
    ships and explicitly marks what it does not.
  grounding: >-
    Spec citations spot-checked (01-PRODUCT-REQUIREMENTS.md L95 slash-command
    sentence, 15-ADAPTER-SPEC.md SS5 expose_command/resume_session at L52/55)
    and the remaining citations (05 SS3/SS7/SS8, 08 SS2/SS4/SS6, 10/11/12/13/
    14/15, 18-TEST-AND-ACCEPTANCE-PLAN.md Scenario B) match the established
    citations of the M13-G02/G03 reviews and the M7/M8/M9/M10 goal reviews;
    cross-document references (START-CLAUDE-CODE.md, CLAUDE-CODE-HANDOFF.md
    section 5) consistent with the repository handoff set.
  determinism: >-
    Documentation is descriptive; the one executable example is proven
    deterministic by live double-run (byte-identical project id and git
    checkpoint under pinned --timestamp/--author identity), consistent with
    the determinism discipline of the M4 init implementation.
  scope: >-
    git status on the head is clean; exactly 5 files added (README.md + 4
    docs under docs/user/ and docs/operations/), all within the goal's allowed
    scope (docs); src/ and tests/ byte-identical to main (diff empty); no
    .development changes, no AC changes, no --no-verify, no src noise. Single
    commit 45e0811 on the dispatch-time base 784e13a; PR mergeable against
    current main a19cce2 (5 files, +819).

decision_notes: >-
  PASS on head 45e08116538b6bffe4b6057b4d52803c63d3a92e with zero findings.
  Independent battery: full suite 3181 passed in 329.61s, ruff clean, mypy 115
  files clean, validator 20/20, CI both jobs PASS; CLI determinism re-verified
  byte-identically against the documented example. All worker findings
  verified accurate: the /goals-no-CLI position is honest and correctly
  grounded (no CLI module exists; planning primitives + RoleContract
  descriptors are the real surface), every Slurm limitation is code-verified
  (constructor signature verbatim, rule-table vocabulary, no paramiko, no
  config/env loading), and the mypy 115 count is independently confirmed
  (OBS-M13-G04-01). Four observations recorded (OBS-M13-G04-01..04), tracked
  for the M13 Release Gate. Proceed to squash merge with --match-head-commit
  45e08116538b6bffe4b6057b4d52803c63d3a92e.
