goal_id: DEV-M5-G01
attempt_id: DEV-M5-G01-a1
reviewer: development-supervisor
decision: PASS
criteria:
  - criterion_id: AC-01
    result: PASS
    evidence: >-
      Duplicate DOI mirrors collapse to one canonical scholarly source:
      test_ac01_duplicate_doi_mirrors_collapse_to_one_canonical_source,
      test_ac01_doi_form_mirrors_collapse_to_one_canonical_source
      (bare/doi:/wrapper-URL forms all collapse), test_ac01_collapsible_
      source_types_share_doi_collapse, test_ac01_distinct_dois_do_not_
      collapse, test_ac01_collapse_pair_verdict_via_rule_c1 (R-DEDUPE-C1).
      Identity groundwork: exhaustive DOI normalization reference grid,
      all-forms-equal grid, canonical identity keys identical across DOI
      forms (test_source_canonical_identity_doi_forms_yield_identical_
      keys). Soundness: 121-pair bi-implication grid test_source_dedupe_
      pair_collapse_iff_equal_canonical_identity proves pair verdict
      SAME_SOURCE iff canonical keys equal, so key-grouped clustering is
      pairwise-consistent. Independent rerun at head 253860ee: tests/
      research 118 passed, exit 0.
  - criterion_id: AC-02
    result: PASS
    evidence: >-
      SI/dataset/structure records remain separately addressable: test_ac02_
      dataset_sharing_paper_doi_remains_separately_addressable, test_ac02_
      si_sharing_paper_doi_remains_separately_addressable, test_ac02_
      structure_deposition_sharing_paper_doi_remains_separately_addressable,
      test_ac02_two_datasets_with_same_doi_do_not_collapse, test_ac02_
      scoping_rule_fires_for_mixed_pairs, test_ac02_dataset_pair_with_
      equal_urls_do_not_collapse. Enforced twice and structurally: (1)
      canonical_identity derives record:<source_id> for RECORD_SCOPED_TYPES
      regardless of any DOI/URL carried; (2) R-DEDUPE-S1 fires before every
      identity rule in the ordered table, so a record-scoped pair can never
      reach a collapse rule. Frozen vocabulary (SourceType) used directly;
      no stringly types.
  - criterion_id: AC-03
    result: PASS
    evidence: >-
      Mirror provenance retained: test_ac03_mirror_provenance_retained_on_
      canonical_source, test_ac03_mirror_urls_retained_on_canonical_source,
      test_ac03_provenance_retained_without_duplicates, test_ac03_singleton_
      retains_own_provenance. R-MERGE-3 collects every distinct provenance
      string and locator URL of every member in input order; the merge keeps
      first-seen canonical fields (R-MERGE-1) and first non-None optionals
      (R-MERGE-2) without discarding member contributions; the auditable
      SourcePairAssessment records exact inputs, both identities and every
      rule decision. Verification rerun at head: full suite 1001 passed / 6
      skipped / 1 deselected (base 883 + 118 new), ruff + mypy clean on 42
      source files, contracts validator PASS.
verification_rerun:
  - command: python -m pytest -q tests/research -k source
    exit_code: 0
  - command: python -m pytest -q tests/research
    exit_code: 0
  - command: python -m pytest -q
    exit_code: 0
  - command: python -m ruff check .
    exit_code: 0
  - command: python -m mypy src/
    exit_code: 0
  - command: python scripts/validate_development_contracts.py
    exit_code: 0
comments:
  - >-
    Scope check: 5 files changed — src/scientific_reproduction/research/
    sources.py, research/dedupe.py, research/__init__.py (package scaffold)
    + tests/research/test_source_normalization.py, test_source_dedupe.py —
    exactly the allowed_scope (research/ + tests/research/**); nothing else
    touched (core/models.py, audit/, artifacts/, planning/, cli/ untouched).
    Frozen models/schemas/goal files untouched; single conventional commit
    [DEV-M5-G01] (branch contains exactly one commit above base 5d8f5c2),
    no amend, no force-push, no tests deleted or weakened (purely additive,
    +118). Merge state CLEAN; PR head equals the reviewed head.
  - >-
    Design reviewed: the dedupe engine faithfully follows the frozen
    core/rules/ paradigm — ordered first-match-wins rule table with a total
    default rule (R-DEDUPE-D1), ruleset version constant DEDUPE_RULESET_
    VERSION="1.0" recorded in every assessment, full auditable decision
    trace, TypeError at public boundaries, pure deterministic predicates.
    The ordered rule table is self-proving for the AC pair: S1 (record-
    scoped) precedes C1/C2/C3, and C2/C3 predicates can only fire when the
    identity derivation guaranteed the claimed dimension (a record carrying
    a DOI never exposes normalized_stable_identifier/URL), so the key-
    equality bi-implication is exact. The frozen spec does not define DOI/
    URL normalization mechanics; the worker documented normative readings
    (lowercase canonical DOI, wrapper forms, URL rules, collapsible family
    = all frozen types except SI/dataset/structure, identity priority
    chain) in module docstrings citing 06-EVIDENCE-SYSTEM.md s7,
    agent-contracts/RESEARCH.md, schemas/source.schema.yaml and core/
    models.py, and locked them with exhaustive grids — appropriate
    normative reading, no contract modification. Deterministic frozen
    inputs only; no randomness, no wall-clock.
  - >-
    Pyright IDE diagnostics on the new files are the established noise
    classes: "unreachable" flags are the defensive TypeError raises on
    typed parameters (paradigm pattern); the default rule predicate
    lambda i: True is the uniform-predicate signature; the two "index 0
    out of range for tuple[()]" flags in deduplicate_sources point at a
    by-construction invariant (a group is created only alongside its first
    member, so members[0] is always defined); the "could not be resolved"
    import flag on research/__init__.py comes from pyright resolving
    against the main repo instead of the worktree. mypy, the configured
    gate, is clean on 42 source files and CI both jobs PASS (Test/lint/
    type check + Validate development contracts). No DEV-DEFECT-002 flake
    occurrence on this PR (local runs and CI green). origin/main moved
    during work by the goal's own dispatch commit (3b567ac, .development-
    only) — verified no in-scope path touched; merge state CLEAN, no
    rebase needed.
git_review:
  pr_number: 23
  reviewed_head_sha: 253860ee5e1bfae1ea662443a0edc00093859f53
  ci_status: PASS
  scope_check: PASS
  merge_authorized: true
  merge_method: squash
