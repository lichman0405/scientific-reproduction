goal_id: DEV-M2-G01
attempt_id: DEV-M2-G01-a1
reviewer: development-supervisor
decision: PASS
criteria:
  - criterion_id: AC-01
    result: PASS
    evidence: >-
      Every normative legal transition is accepted by both the
      type-specific predicates (is_legal_* / apply_*) and the generic
      API (can_transition / transition): project mainline
      INITIALIZING -> ... -> COMPLETED (9 arcs), the replanning loop
      EXECUTING -> REPLANNING -> PLAN_AUDIT, the full suspension
      cross-product (10 active phases <-> PAUSED / WAITING_HUMAN /
      WAITING_RESOURCE), the Run mainline CREATED -> ... -> CLOSED
      (7 arcs), pre-result cancellation (CREATED/READY/DISPATCHED/
      RUNNING_EXTERNAL -> CANCELLED) and result-bearing invalidation
      (RESULT_AVAILABLE/ANALYZING/SUBMITTED_FOR_REVIEW -> INVALIDATED).
      Test evidence: test_run_mainline_transitions_accepted,
      test_run_cancellation_arcs_accepted, test_run_invalidation_arcs_
      accepted plus the project-phase equivalents and the generic
      dispatch tests (76 lifecycle tests pass at head). Independent
      rerun in a fresh venv installed from the worktree at head
      01f2fa0: 76 passed, exit 0; import path verified to resolve into
      .worktrees/dev-m2-g01 (no base-code contamination).
  - criterion_id: AC-02
    result: PASS
    evidence: >-
      Representative illegal shortcuts are rejected with
      IllegalTransitionError carrying subject/from_state/to_state: the
      named shortcut RUNNING_EXTERNAL -> CREATED, stage skips and
      backwards moves, terminal escapes (COMPLETED -> EXECUTING,
      CLOSED -> READY), wrong abort kinds (RESULT_AVAILABLE -> CANCELLED,
      CREATED -> INVALIDATED) and same-state no-ops. Unregistered enum
      types, mixed enum types and plain strings raise TypeError before
      any table lookup (transitions.py facade). Verification rerun at
      head: 76 passed, exit 0.
  - criterion_id: AC-03
    result: PASS
    evidence: >-
      Deterministic and not prompt-dependent: both rule tables are
      immutable frozensets of exact enum pairs; the Run table (14 arcs)
      is written out literally in the test file; the project table
      (71 arcs) is rebuilt from locked literals in test_lifecycle_
      project.py; tables are identical after module reimport; every
      decision is a pure function of (state, new_state); no LLM, no
      randomness, no wall-clock dependence anywhere in the module
      (verified by code review); tables hold only frozen
      LifecycleState/ProjectPhase members (no outcome/review/
      plan-status leakage). Verification rerun at head: full suite
      546 passed / 6 skipped / 1 deselected, ruff + mypy clean on 32
      source files, contracts validator PASS (14 milestones, 80 goals,
      both DAGs acyclic).
verification_rerun:
  - command: python -m pytest -q tests/rules -k lifecycle
    exit_code: 0
  - command: python -m pytest -q tests/rules
    exit_code: 0
  - command: python -m pytest -q
    exit_code: 0
  - command: python -m ruff check .
    exit_code: 0
  - command: python -m mypy src/
    exit_code: 0
  - command: python scripts/validate_development_contracts.py
    exit_code: 0
comments:
  - >-
    Scope check: 6 files changed — core/transitions.py,
    core/rules/lifecycle.py, core/rules/__init__.py,
    tests/rules/test_lifecycle_{project,run,shared}.py — all inside
    allowed_scope; core/rules/__init__.py created here as contracted
    (G01 owns it; G03/G04/G07 deliberately did not create one);
    frozen paths untouched; conventional commit [DEV-M2-G01], no
    force-push, no amend; no tests deleted or weakened.
  - >-
    Design reviewed: the spec contains no explicit arc table; the
    worker's normative reading is documented in the module docstring
    and locked by exact-data tests — old==new is never legal (no-op
    transitions stay out of the audit log), RESULT_AVAILABLE ->
    CANCELLED is illegal (invalidate instead), CREATED -> INVALIDATED
    is illegal, suspension states cannot transition to each other,
    resume goes to any active phase (a pair table cannot remember the
    paused-from phase — deterministically encoded), terminal states
    (COMPLETED / CLOSED / CANCELLED / INVALIDATED) have no outgoing
    arcs. The generic transitions.py facade dispatches on enum type,
    rejects unregistered/mixed/string inputs, and raises
    IllegalTransitionError with a stable subject string
    ('project-phase' / 'run-lifecycle'). Consistent with the frozen
    enums in core/models.py (ProjectPhase 14 members, LifecycleState
    10 members) — no schema/model changes.
  - >-
    Verified independently in a fresh venv installed from the worktree
    (not the repo root): python -c import check printed the worktree
    path for core.rules.lifecycle; all suites rerun at the exact
    reviewed head 01f2fa0dad460d57993fed6681907c39e462102b which also
    equals the PR head per gh (state OPEN, mergeStateStatus CLEAN, CI
    both jobs PASS).
git_review:
  pr_number: 14
  reviewed_head_sha: 01f2fa0dad460d57993fed6681907c39e462102b
  ci_status: PASS
  scope_check: PASS
  merge_authorized: true
  merge_method: squash
