goal_id: DEV-M4-G01
attempt_id: DEV-M4-G01-a1
reviewer: development-supervisor
decision: PASS
criteria:
  - criterion_id: AC-01
    result: PASS
    evidence: >-
      One Primary Target Paper enforced per project, twice and
      structurally: (1) the frozen Project model requires exactly one
      primary_target object and every state write is schema-validated via
      validate_and_reject before hitting disk; (2) behavioral enforcement
      with stable errors — test_init_writes_exactly_one_primary_target,
      test_init_rejects_second_primary_target_registration (same message
      for same inputs, state untouched), test_init_rejects_second_
      initialization_of_same_project, test_init_rejects_primary_
      registration_before_initialization. register_primary_target rejects
      with TargetAlreadyRegisteredError naming the project id and existing
      identifier; the success path is documented as reachable only on
      hand-edited state (init always registers), an honest normative
      reading of the enforcement-point API. Independent rerun at head
      6a87fc26: tests/planning -k init 30 passed, exit 0.
  - criterion_id: AC-02
    result: PASS
    evidence: >-
      Initialization requires no lab/HPC inventory and no network: the
      init path creates the 21-directory INIT_DIRECTORIES tree, writes
      project.yaml through core.atomic.atomic_write, appends the
      project.initialized event, and records Git state through the M3-G01
      helpers (audit.git.init_project_repo + commit_checkpoint) — all
      offline and deterministic. test_init_works_without_lab_hpc_
      inventory (bare nested root, empty inventory/, no inventory refs in
      state), test_init_creates_project_directories, test_init_
      initializes_git_repository, test_init_commits_project_initialized_
      checkpoint (deterministic identity/date via git show), test_init_
      writes_project_state_file (exact dict with fixed timestamp),
      test_init_writes_project_initialized_event, test_init_state_
      contents_are_deterministic (identical bytes incl. project_id and
      commit SHA across two roots), test_init_state_validates_against_
      frozen_schema, test_init_project_id_is_deterministic.
  - criterion_id: AC-03
    result: PASS
    evidence: >-
      All three target forms represented losslessly in the frozen
      PrimaryTarget/TargetSourceType model (pdf/doi/url): test_init_
      target_form_pdf_represented, test_init_target_form_doi_represented
      (identifier + doi both recorded, mirroring examples/fdm-201/
      project.example.yaml), test_init_target_form_url_represented,
      test_init_target_forms_roundtrip_into_project_state (all 3 forms
      through state). Malformed forms rejected with stable
      TargetValidationError messages: test_init_rejects_malformed_doi
      (5 cases), test_init_rejects_unparsable_url (4 cases), test_init_
      rejects_undeterminable_target_form, test_init_rejects_explicit_
      form_mismatch. Determinism (AC-02/03 shared): injectable
      timestamps (naive rejected), canonical JSON, deterministic
      generate_id("project", source_type, identifier), pinned
      AuditIdentity. Verification rerun at head: full suite 913 passed /
      6 skipped / 1 deselected (base 883 + 30 new), ruff + mypy clean on
      42 source files, contracts validator PASS.
verification_rerun:
  - command: python -m pytest -q tests/planning -k init
    exit_code: 0
  - command: python -m pytest -q tests/planning
    exit_code: 0
  - command: python -m pytest -q
    exit_code: 0
  - command: python -m ruff check .
    exit_code: 0
  - command: python -m mypy src/
    exit_code: 0
  - command: python scripts/validate_development_contracts.py
    exit_code: 0
comments:
  - >-
    Scope check: 6 files changed — src/scientific_reproduction/planning/
    init.py (new), planning/__init__.py (package scaffold + re-exports),
    cli/reproduce.py (new), cli/__init__.py (docstring only) +
    tests/planning/test_init.py, tests/planning/init_helpers.py — exactly
    the allowed_scope (planning/init.py + cli/** + tests/planning/**);
    core/models.py, audit/, artifacts/, research/ untouched. Frozen
    models/schemas/goal files untouched; single conventional commit
    [DEV-M4-G01] (branch contains exactly one commit above merge base
    3b567ac), no amend, no force-push, no tests deleted or weakened
    (purely additive, +30). Merge state CLEAN/MERGEABLE against current
    main; PR head equals the reviewed head.
  - >-
    Design reviewed: initialize_project composes the M3-G01 git helpers
    (init_project_repo + commit_checkpoint with explicit identity and
    injectable commit_time) rather than re-implementing git, honors the
    M3-G03 boundary correctly (no manifest/inventory at init — the frozen
    spec's acquisition phase follows init per 01-PRODUCT-REQUIREMENTS SS4;
    inventory/ created empty). Frozen vocabulary used directly
    (ProjectPhase.INITIALIZING, ReproductionOutcome.UNDETERMINED,
    TargetSourceType; state_backend Literal["filesystem"] per schema
    const). Spec-underspecified points resolved as documented normative
    readings in the module docstring: project.yaml holds canonical JSON
    (JSON is valid YAML — honors the template name while staying
    stdlib-only and consistent with 14-STATE-GIT-ARTIFACTS SS2/SS3);
    INITIAL_PLAN_VERSION "v1-draft" matches examples/fdm-201/project
    .example.yaml; PDF existence deliberately not required at init
    (acquisition is a later phase); init event written directly as
    events/<event_id>.json per the workspace template layout with the
    later-milestone wiring concern (ProjectEventLog base-dir convention)
    documented. Paradigm: TypeError at public boundaries (wrong types
    rejected before state checks), ValueError-subclass errors with stable
    messages, frozen ProjectInitResult, __all__ exports, docstrings
    citing frozen specs. Deterministic frozen inputs only; the now-UTC
    default appears only where the frozen model requires a timestamp and
    is injectable for tests.
  - >-
    Pyright IDE diagnostics on the new files are the established noise
    classes: "unreachable" flags are the defensive TypeError raises on
    typed parameters and the unreachable trailing parser.error branch in
    main (argparse subparsers are required); "parser is not accessed" is
    the local assigned for parse-time side effects; the "Argument of type
    str cannot be assigned to state_backend Literal['filesystem']"
    flag is a stale-snapshot diagnostic (the file passes DEFAULT_STATE_
    BACKEND which is already typed Literal["filesystem"]; mypy confirms
    the current file is clean); the import-resolution flags on
    tests/planning/init_helpers.py come from pyright resolving against
    the main repo instead of the worktree. mypy, the configured gate, is
    clean on 42 source files and CI both jobs PASS (Test/lint/type check
    + Validate development contracts). No DEV-DEFECT-002 flake occurrence
    on this PR (local runs and CI green). origin/main moved during work
    by this batch's own state/dispatch and M5-G01 merge commits
    (.development + research scopes only) — verified no in-scope path
    touched; mergeStateStatus CLEAN, no rebase needed.
git_review:
  pr_number: 24
  reviewed_head_sha: 6a87fc26a397f6cf187d2422c7f7134586415547
  ci_status: PASS
  scope_check: PASS
  merge_authorized: true
  merge_method: squash
