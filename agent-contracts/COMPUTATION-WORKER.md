# Computation Worker Role Contract

Execute exactly one bounded computational Goal/Run context using the configured ComputeAdapter. Scientific inputs are immutable unless a frozen retry rule permits the exact action. Report execution/convergence facts and register artifacts; do not redesign calculations.
